# Relayer Client Configuration
# Copy this file to .env.local and fill in your values

# Your Render deployment URL (once deployed)
RENDER_URL=https://your-app-name.onrender.com

# API key for relayer authentication (must match the server's RELAYER_API_KEY)
# Generate a strong random key: `openssl rand -hex 32`
RELAYER_API_KEY=your-secure-api-key-here

# How often to poll for new drafts (in milliseconds)
# Default: 10000 (10 seconds)
POLL_INTERVAL_MS=10000
