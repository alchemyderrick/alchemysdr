# Railway Backend Service Configuration
# This deploys the Express API server

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "sdr-console-backend"

[services.healthcheck]
path = "/api/health/claude"
timeout = 100
interval = 30
