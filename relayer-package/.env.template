# ============================================
# SDR Console Relayer Client Configuration
# ============================================

# Your unique employee identifier (use your first name or initials)
# Examples: john, jane, derrick, jsmith
# This MUST be unique to you and match your login username
EMPLOYEE_ID=your_name_here

# Railway server URL (SAME for all employees)
RENDER_URL=https://sdr-console-production.up.railway.app

# API key for authentication (provided by admin - SAME for all employees)
RELAYER_API_KEY=your_api_key_here

# Polling interval in milliseconds (how often to check for new drafts)
# Default: 10000 (10 seconds)
POLL_INTERVAL_MS=10000

# ============================================
# IMPORTANT NOTES:
# ============================================
# - EMPLOYEE_ID must be UNIQUE to you (your name/initials from admin)
# - RENDER_URL is the same for all employees
# - RELAYER_API_KEY is the same for all employees (provided by admin)
# - Do NOT add ANTHROPIC_API_KEY or APOLLO_API_KEY here
#   (those are already configured on Railway)
