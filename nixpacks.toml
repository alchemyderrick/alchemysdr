# Nixpacks configuration for Railway
# Node.js version is detected from .nvmrc file

[phases.setup]
# Only add Chromium, let Railway detect Node.js from .nvmrc
nixPkgs = ["chromium"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
