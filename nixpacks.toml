[phases.setup]
nixPkgs = ["nodejs-20_x", "chromium"]

[phases.install]
cmds = ["npm ci"]

# Note: Build is handled in railway.toml startCommand
# This ensures frontend is built before server starts

[start]
cmd = "npm run build && npm start"
