# ============================================
# SDR Console - Server Environment Variables
# ============================================

# AI & Research API Keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here
APOLLO_API_KEY=your_apollo_api_key_here

# Server Configuration
NODE_ENV=production
PORT=3000

# Session Secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your_session_secret_here

# Relayer Client Authentication
# This key is shared by all employee relayer clients
# Generate with: openssl rand -hex 32
RELAYER_API_KEY=your_relayer_api_key_here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3001

# Database Path (optional - defaults to data.db)
# Note: Employee databases are automatically created in databases/{employeeId}/
# DB_PATH=data.db

# X/Twitter Authentication (optional - per-employee storage preferred)
# Employees should authenticate via the web UI instead
# X_COOKIES=

# Clay Webhook (optional)
# CLAY_WEBHOOK_URL=

# Telegram Auto-send Configuration
# AUTO_SEND_ENABLED=true
# AUTO_SEND_IDLE_SECONDS=5

# ============================================
# IMPORTANT NOTES FOR MULTI-EMPLOYEE SETUP:
# ============================================
#
# - SESSION_SECRET: Required for authentication
# - RELAYER_API_KEY: Shared by all employee Mac clients
# - ANTHROPIC_API_KEY & APOLLO_API_KEY: Shared company resources
# - Do NOT set X_COOKIES here - employees authenticate individually via web UI
# - Employee databases are auto-created in databases/{employeeId}/data.db
#
# For Railway deployment, set these in Railway dashboard.
# For local development, copy this to .env and fill in values.
